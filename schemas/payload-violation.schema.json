{
  "additionalProperties": false,
  "properties": {
    "action_sha256": {
      "anyOf": [
        {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Action Sha256"
    },
    "decision_hash": {
      "anyOf": [
        {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Decision Hash"
    },
    "raw_bytes": {
      "anyOf": [
        {
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Raw Bytes"
    },
    "raw_sha256": {
      "anyOf": [
        {
          "pattern": "^[0-9a-f]{64}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Raw Sha256"
    },
    "reason_code": {
      "pattern": "^[a-z0-9](?:[a-z0-9._:-]{0,127})$",
      "title": "Reason Code",
      "type": "string"
    }
  },
  "required": [
    "reason_code"
  ],
  "title": "ViolationEventPayload",
  "type": "object"
}
